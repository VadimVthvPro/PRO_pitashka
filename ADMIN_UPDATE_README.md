# 🔐 Администратор базы данных PROпиташка - Обновление

## 📋 Что нового

### ✅ Реализовано

1. **Расширенный список таблиц** - добавлены все таблицы базы данных:
   - 👤 Пользователи (`user_main`)
   - 🍽️ Питание (`food`)
   - 🎯 Цели (`user_aims`)
   - 💪 Здоровье (`user_health`)
   - 🌐 Языки (`user_lang`)
   - 🏃 Тренировки (`user_training`)
   - 💧 Вода (`water`)
   - 🏋️ Типы тренировок (`training_types`) **[НОВОЕ]**
   - 📊 Коэффициенты (`training_coefficients`) **[НОВОЕ]**
   - 💬 История чата (`chat_history`) **[НОВОЕ]**
   - 👨‍💼 Администраторы (`admin_users`) **[НОВОЕ]**

2. **Scrollable Tabs (прокручиваемые вкладки)**
   - При большом количестве таблиц вкладки можно прокручивать горизонтально
   - Удобная навигация между всеми таблицами

3. **Полная локализация на русский язык**
   - Все названия колонок переведены на понятный русский
   - Кнопки и сообщения на русском языке
   - Эмодзи для лучшей визуализации

4. **Каскадное удаление**
   - При удалении пользователя из таблицы "Пользователи" автоматически удаляются все связанные записи:
     - История чата
     - Записи питания
     - Цели
     - Данные здоровья
     - Языковые настройки
     - Тренировки
     - Учет воды

5. **Расширенный функционал**
   - ✅ Просмотр всех таблиц
   - ✅ Фильтрация данных
   - ✅ Сортировка по колонкам
   - ✅ Добавление записей с валидацией
   - ✅ Редактирование ячеек (двойной клик)
   - ✅ Удаление записей
   - ✅ Счетчик записей
   - ✅ Подсветка измененных/добавленных записей

6. **Улучшенный интерфейс**
   - Увеличенное окно (1400x900)
   - Минимальный размер окна (1000x700)
   - Масштабируемость всех элементов
   - Прокрутка для форм с большим количеством полей

## 🚀 Установка и запуск

### Шаг 1: Применить миграцию

```bash
psql -U postgres -d propitashka -f migrations/003_create_admin_users.sql
```

Это создаст:
- Таблицу `admin_users`
- Администратора по умолчанию (логин: `admin`, пароль: `admin`)

### Шаг 2: Настроить подключение к БД

Убедитесь, что в файле `.env` указаны корректные данные:

```env
ADMIN_DB_USER=propitashka_admin
ADMIN_DB_PASSWORD=your_admin_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=propitashka
```

### Шаг 3: Запустить администратор

#### Вариант A: Готовое приложение (macOS)

```bash
open "PROpitashka Admin.app"
```

#### Вариант B: Из исходников

```bash
python admin_of_bases.py
```

### Шаг 4: Войти в систему

- **Логин:** `admin`
- **Пароль:** `admin`

⚠️ **ВАЖНО:** Сразу после входа смените пароль!

## 🔑 Управление администраторами

### Создать нового администратора

```bash
python create_admin.py
```

Или с параметрами:

```bash
python create_admin.py myusername mypassword
```

### Изменить пароль существующего администратора

```bash
python create_admin.py admin new_password
```

### Удалить администратора (через SQL)

```sql
DELETE FROM admin_users WHERE username = 'admin';
```

## 📊 Особенности работы с таблицами

### Пользователи (user_main)
- **Каскадное удаление:** При удалении пользователя удаляются все его данные
- **Возраст:** Вводится возраст (5-100 лет), а не дата рождения
- **Пол:** Мужской / Женский

### Типы тренировок (training_types)
- **Мультиязычность:** Названия на 5 языках (RU, EN, DE, FR, ES)
- **Коэффициенты:** Базовый коэффициент расхода калорий
- **Активность:** Можно отключать неиспользуемые тренировки

### Коэффициенты (training_coefficients)
- **Модификаторы:** По полу, возрасту, весу, росту
- **Точность:** До 3 знаков после запятой
- **Ограничение:** Нельзя добавлять через интерфейс (создаются автоматически)

### История чата (chat_history)
- **Тип сообщения:** user (пользователь) или bot (бот)
- **Лимит:** Показываются последние 1000 сообщений
- **Текст:** Обрезается до 100 символов в списке

### Администраторы (admin_users)
- **Хеширование:** Пароли хранятся в виде bcrypt-хешей
- **Ограничение:** Нельзя добавлять через интерфейс (используйте `create_admin.py`)
- **Безопасность:** Хеш пароля нельзя редактировать напрямую

## 🎨 Горячие клавиши

- **Двойной клик** на ячейке - редактирование
- **Enter** - сохранить изменения
- **Escape** - отменить редактирование
- **Клик вне поля** - отменить редактирование

## 🔒 Безопасность

1. ⚠️ **Смените пароль по умолчанию сразу после установки**
2. 🔐 Используйте сильные пароли (минимум 8 символов, буквы + цифры)
3. 🚫 Не храните пароли в открытом виде
4. 📝 Регулярно проверяйте таблицу `admin_users`
5. 🔍 Следите за полем `last_login_at` для выявления подозрительной активности

## 📝 Примеры использования

### Найти всех пользователей из России

1. Откройте вкладку "🌐 Языки"
2. В фильтре введите: `ru`
3. Нажмите "Искать"

### Посмотреть тренировки конкретного пользователя

1. Откройте вкладку "🏃 Тренировки"
2. В фильтре введите ID пользователя
3. Нажмите "Искать"

### Удалить пользователя со всеми данными

1. Откройте вкладку "👤 Пользователи"
2. Выберите пользователя (щелкните на строку)
3. Нажмите "🗑️ Удалить строку"
4. Подтвердите удаление
5. ✅ Все связанные данные удалятся автоматически

### Добавить новый тип тренировки

1. Откройте вкладку "🏋️ Типы тренировок"
2. Нажмите "➕ Добавить запись"
3. Заполните все поля на 5 языках
4. Нажмите "✅ Сохранить"

## 🐛 Решение проблем

### Ошибка "Таблица admin_users не существует"

**Решение:**
```bash
psql -U postgres -d propitashka -f migrations/003_create_admin_users.sql
```

### Ошибка "Неверное имя пользователя или пароль"

**Варианты:**
1. Проверьте регистр символов (чувствительно к регистру)
2. Убедитесь, что миграция применена
3. Проверьте, что администратор существует:
   ```sql
   SELECT * FROM admin_users;
   ```

### Ошибка подключения к БД

**Проверьте:**
1. PostgreSQL запущен: `pg_isready`
2. База данных существует: `psql -l | grep propitashka`
3. Данные в `.env` корректны
4. Пользователь `propitashka_admin` существует и имеет права

### Окно слишком маленькое

**Решение:**
- Растяните окно мышью (минимум 1000x700)
- Рекомендуемый размер: 1400x900

## 📚 Техническая информация

- **Язык:** Python 3.12
- **GUI:** Tkinter
- **База данных:** PostgreSQL 14+
- **Хеширование:** bcrypt
- **Кодировка:** UTF-8

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f bot.log`
2. Проверьте подключение к БД
3. Убедитесь, что все миграции применены
4. Проверьте права пользователя БД

## 📄 Лицензия

Часть проекта PROпиташka

