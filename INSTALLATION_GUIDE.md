# 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО УСТАНОВКЕ

## Система Тренировок V2.0 для PROpitashka Bot

---

## ⏱ Время установки: ~3 минуты

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

Убедитесь, что у вас установлено:
- ✅ Python 3.12+
- ✅ PostgreSQL 12+
- ✅ psycopg2
- ✅ aiogram 3.x

---

## 🔍 ШАГ 0: Проверка готовности

Перед установкой проверьте, что все файлы на месте:

```bash
cd /Users/VadimVthv/Desktop/PROpitashka
./check_training_system.sh
```

**Ожидаемый результат:**
```
✅ Пройдено: 29+
❌ Провалено: 0
🎉 ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ!
```

Если есть ошибки - проверьте наличие файлов в документации.

---

## 📦 ШАГ 1: Миграция базы данных (1 минута)

### Вариант A: Через psql (рекомендуется)

```bash
psql -U postgres -d propitashka -f migrations/001_create_training_system.sql
```

### Вариант B: Через pgAdmin

1. Откройте pgAdmin
2. Подключитесь к базе `propitashka`
3. Откройте Query Tool
4. Загрузите файл `migrations/001_create_training_system.sql`
5. Нажмите Execute (F5)

### Проверка успешности миграции:

```sql
-- Проверьте наличие таблиц
SELECT COUNT(*) FROM training_types;  -- Должно быть 20
SELECT COUNT(*) FROM training_coefficients;  -- Должно быть 40+

-- Проверьте наличие функций
SELECT proname FROM pg_proc WHERE proname LIKE 'calculate%';
```

**Ожидаемый результат:**
- 20 тренировок в `training_types`
- 40+ коэффициентов в `training_coefficients`
- Функция `calculate_calories_burned` создана
- Колонки `training_type_id` и `training_name` добавлены в `user_training`

---

## 🔧 ШАГ 2: Интеграция в main.py (30 секунд)

### Автоматическая интеграция (рекомендуется):

```bash
python3 integrate_workout_system.py
```

**Что делает скрипт:**
1. ✅ Создает backup `main.py` → `main.py.backup`
2. ✅ Создает backup `keyboards.py` → `keyboards.py.backup`
3. ✅ Добавляет импорты для новой системы
4. ✅ Регистрирует `workout_router`
5. ✅ Удаляет старые обработчики `tren()`, `tren_type()`
6. ✅ Удаляет старую функцию `intensiv()`
7. ✅ Комментирует состояние `REG.types`
8. ✅ Удаляет старую клавиатуру `tren`

**Ожидаемый вывод:**
```
✅ Backup создан: main.py.backup
✅ Backup создан: keyboards.py.backup
✅ Импорты добавлены
✅ Роутер зарегистрирован
✅ Старый код удален
✅ Интеграция завершена успешно!
```

### Ручная интеграция (если автоматическая не сработала):

См. детальные инструкции в `TRAINING_SYSTEM_V2_INTEGRATION.md`

---

## 🎨 ШАГ 3: Компиляция локализации (если еще не сделано)

```bash
# Для всех языков
for lang in ru en de fr es; do
    msgfmt -o locales/$lang/LC_MESSAGES/messages.mo locales/$lang/LC_MESSAGES/messages.po
done
```

**Проверка:**
```bash
ls -lh locales/*/LC_MESSAGES/messages.mo
```

Должны быть файлы `.mo` для всех 5 языков.

---

## 🚀 ШАГ 4: Запуск бота

```bash
python3 main.py
```

**Ожидаемый вывод:**
```
INFO: Bot started successfully
INFO: Registered workout_router
INFO: Listening for updates...
```

**⚠️ Важно:** Убедитесь, что нет других запущенных экземпляров бота!

Если видите ошибку:
```
TelegramConflictError: terminated by other getUpdates request
```

Остановите все другие экземпляры:
```bash
pkill -f "python.*main.py"
# Затем запустите снова
python3 main.py
```

---

## ✅ ШАГ 5: Тестирование (5-10 минут)

### Базовый тест:

1. **Откройте бота в Telegram**
2. **Нажмите "Добавить тренировки"** (или аналог на вашем языке)
3. **Проверьте:**
   - ✅ Отображается список тренировок с эмодзи
   - ✅ Есть кнопки навигации (⬅️ ➡️)
   - ✅ Есть кнопка "Главное меню"
   - ✅ Отображается индикатор страниц (Страница 1/4)

4. **Выберите тренировку** (например, 🏃 Бег)
5. **Проверьте:**
   - ✅ Отображается запрос длительности
   - ✅ Есть кнопка отмены

6. **Введите длительность** (например, 30)
7. **Проверьте:**
   - ✅ Если нет веса - запрашивается вес
   - ✅ Если вес есть - сразу расчет

8. **После расчета проверьте:**
   - ✅ Отображается название тренировки
   - ✅ Отображается длительность
   - ✅ Отображаются сожженные калории
   - ✅ Отображается суммарное количество за день

### Расширенный тест:

1. **Протестируйте пагинацию:**
   - Нажмите "➡️" - должна открыться страница 2
   - Нажмите "⬅️" - должна открыться страница 1

2. **Протестируйте возврат:**
   - Нажмите "Главное меню" - должны вернуться в меню

3. **Протестируйте отмену:**
   - Выберите тренировку
   - Нажмите "Отмена" - должны вернуться в меню

4. **Протестируйте валидацию:**
   - Введите длительность "abc" - должна быть ошибка
   - Введите длительность "500" - должна быть ошибка
   - Введите длительность "30" - должно работать

5. **Протестируйте разные тренировки:**
   - Попробуйте 2-3 разные тренировки
   - Убедитесь, что калории различаются

### Тест на разных языках:

Смените язык в настройках бота и повторите базовый тест для:
- 🇷🇺 Русский
- 🇬🇧 English
- 🇩🇪 Deutsch (использует fallback на EN)
- 🇫🇷 Français (использует fallback на EN)
- 🇪🇸 Español (использует fallback на EN)

---

## 🐛 ШАГ 6: Проверка логов

```bash
tail -f bot.log | grep -i workout
```

**Ожидаемые логи:**
```
INFO: User 12345 entered workout selection, showing page 0
INFO: User 12345 navigated to workout page 1
INFO: User 12345 selected workout Бег (1), asking for duration
INFO: User 12345 completed workout Бег: 250.5 kcal burned in 30 min
```

**Проверка ошибок:**
```bash
grep -i error bot.log | tail -20
```

Не должно быть ошибок, связанных с workout/training.

---

## 📊 ШАГ 7: Проверка данных в БД

```sql
-- Проверить сохраненные тренировки
SELECT 
    user_id,
    date,
    training_name,
    tren_time,
    training_cal
FROM user_training
WHERE date = CURRENT_DATE
ORDER BY date DESC
LIMIT 10;

-- Проверить статистику
SELECT 
    training_name,
    COUNT(*) as count,
    AVG(training_cal) as avg_calories,
    AVG(tren_time) as avg_duration
FROM user_training
WHERE training_type_id IS NOT NULL
GROUP BY training_name
ORDER BY count DESC;
```

**Ожидаемый результат:**
- Тренировки сохраняются с правильными названиями
- `training_type_id` заполнен
- `training_name` на русском (или другом языке)
- Калории рассчитаны корректно

---

## ✅ ЧЕКЛИСТ УСПЕШНОЙ УСТАНОВКИ

Отметьте, что все работает:

### База данных:
- [ ] Миграция выполнена без ошибок
- [ ] 20 тренировок в `training_types`
- [ ] 40+ коэффициентов в `training_coefficients`
- [ ] Колонки добавлены в `user_training`

### Интеграция:
- [ ] Скрипт интеграции выполнен
- [ ] Backup файлы созданы
- [ ] Старый код удален
- [ ] Новые импорты добавлены

### Локализация:
- [ ] .mo файлы скомпилированы
- [ ] Ключи обновлены

### Функциональность:
- [ ] Бот запускается без ошибок
- [ ] Список тренировок отображается
- [ ] Пагинация работает
- [ ] Выбор тренировки работает
- [ ] Ввод длительности работает
- [ ] Расчет калорий работает
- [ ] Сохранение в БД работает
- [ ] Результаты отображаются

### Тестирование:
- [ ] Базовый тест пройден
- [ ] Тест на русском пройден
- [ ] Тест на английском пройден
- [ ] Логи чистые (нет ошибок)
- [ ] Данные в БД корректные

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Миграция не выполняется

**Ошибка:** `psql: command not found`

**Решение:**
```bash
# Для macOS с Homebrew
brew install postgresql

# Для Ubuntu/Debian
sudo apt-get install postgresql-client
```

---

### Проблема: Ошибка при импорте workout_router

**Ошибка:** `ModuleNotFoundError: No module named 'app.presentation.bot.routers.workout_handlers'`

**Решение:**
```bash
# Проверьте наличие __init__.py файлов
ls -la app/__init__.py
ls -la app/presentation/__init__.py
ls -la app/presentation/bot/__init__.py
ls -la app/presentation/bot/routers/__init__.py

# Если отсутствуют, создайте:
touch app/__init__.py
touch app/presentation/__init__.py
touch app/presentation/bot/__init__.py
touch app/presentation/bot/routers/__init__.py
```

---

### Проблема: Список тренировок пустой

**Ошибка:** "Список тренировок временно недоступен"

**Решение:**
```sql
-- Проверьте наличие данных
SELECT COUNT(*) FROM training_types;

-- Если 0, повторите миграцию
\i migrations/001_create_training_system.sql
```

---

### Проблема: Калории не рассчитываются

**Ошибка:** "Не удалось рассчитать калории"

**Решение:**
```sql
-- Проверьте функцию расчета
SELECT calculate_calories_burned(1, 70, 30, 'male', 25, 175);

-- Если ошибка, пересоздайте функцию из миграции
```

---

### Проблема: Локализация не работает

**Симптомы:** Отображаются ключи вместо текста (например, "TrenType")

**Решение:**
```bash
# Перекомпилируйте .mo файлы
for lang in ru en de fr es; do
    msgfmt -o locales/$lang/LC_MESSAGES/messages.mo locales/$lang/LC_MESSAGES/messages.po
done

# Перезапустите бота
pkill -f "python.*main.py"
python3 main.py
```

---

### Проблема: Бот не запускается (Conflict error)

**Ошибка:** `TelegramConflictError: terminated by other getUpdates request`

**Решение:**
```bash
# Остановите все экземпляры бота
pkill -f "python.*main.py"

# Подождите 5 секунд
sleep 5

# Запустите снова
python3 main.py
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- 📖 `QUICKSTART_TRAINING_V2.md` - Быстрый старт
- 📘 `TRAINING_SYSTEM_V2_INTEGRATION.md` - Полная документация
- 📙 `LOCALIZATION_KEYS_V2.md` - Ключи локализации
- 📗 `FINAL_REPORT.md` - Технический отчет
- 📕 `TASKS_COMPLETED_SUMMARY.md` - Сводка задач

---

## ✅ ГОТОВО!

Если все чеклисты отмечены ✅ - **поздравляем!** 🎉

Система тренировок V2.0 успешно установлена и готова к использованию!

**Наслаждайтесь новой функциональностью! 💪🏋️🎯**

---

*Дата создания: 8 ноября 2025*  
*Версия: 2.0 Final*  
*Время установки: ~3 минуты*


