# 🔐 Вход в администратор PROпиташка

## Учетные данные по умолчанию

**Логин:** `admin`  
**Пароль:** `admin`

---

## ⚡ Быстрый старт

### 1. Создать таблицу администраторов (если не создана)

```bash
psql -U postgres -d propitashka -f migrations/003_create_admin_users.sql
```

### 2. Запустить администратор

**Вариант A (готовое приложение macOS):**
```bash
open "PROpitashka Admin.app"
```

**Вариант B (из исходников):**
```bash
python admin_of_bases.py
```

### 3. Войти
- Логин: `admin`
- Пароль: `admin`

---

## 🔑 Создать нового администратора

```bash
python create_admin.py
```

Или с параметрами:
```bash
python create_admin.py username password
```

---

## 🛠️ Изменить пароль

```bash
python create_admin.py admin новый_пароль
```

---

## ⚠️ ВАЖНО

**Сразу после первого входа смените пароль по умолчанию!**

Используйте скрипт:
```bash
python create_admin.py admin ваш_надежный_пароль
```

---

## 📋 Что доступно в панели

✅ **11 таблиц базы данных:**
- 👤 Пользователи
- 🍽️ Питание
- 🎯 Цели
- 💪 Здоровье
- 🌐 Языки
- 🏃 Тренировки
- 💧 Вода
- 🏋️ Типы тренировок
- 📊 Коэффициенты
- 💬 История чата
- 👨‍💼 Администраторы

✅ **Функции:**
- Просмотр данных
- Фильтрация и поиск
- Сортировка по колонкам
- Добавление записей
- Редактирование (двойной клик)
- Удаление с каскадом
- Счетчик записей

---

## 🆘 Проблемы?

### "Таблица admin_users не существует"
```bash
psql -U postgres -d propitashka -f migrations/003_create_admin_users.sql
```

### "Неверный логин или пароль"
Проверьте, что администратор создан:
```sql
psql -U postgres -d propitashka -c "SELECT username FROM admin_users;"
```

### Забыли пароль?
Создайте нового администратора:
```bash
python create_admin.py newadmin newpassword
```

---

**Документация:** см. `ADMIN_UPDATE_README.md`

